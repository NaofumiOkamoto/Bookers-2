<div class="body">
  <div class="container">
    <% if @book.errors.any? %>
      <%= @book.errors.count %>error prohibited this obj from being saved:
        <% @book.errors.full_messages.each do |message| %>
          <%= message %>
        <% end %>
    <% end %>
    <div class="col-md-6 col-md-offset-3">
      <h1>Editing Book</h1>
      <%= form_for [@book] do |f| %>
      <label class="">Title</label>
        <p><%= f.text_field :title, class:"book_edit_field" %></p>
      <label class="">Title</label></p>
        <p><%= f.text_area :body, class:"book_edit_field"%></p>
        <div class="update_book_submit">
        <p><%= f.submit 'update book', class:"btn btn-primary col-xs-1"%></p>
        </div>
      <% end %>
      <%= link_to 'Show', book_path(params[:id]) %> |
      <%= link_to 'Back', books_path %>
    </div>
  </div>
</div>

