<p>本のindexページ</p>
<div class="">
    <table class="table table-hover table-inverse">
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
            <% @books.each do |book| %>
              <tr>
                <td><%= book.user_id %></td>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
              </tr>
            <% end %>
    </table>
</div>
